export interface Brand {
    id: string;
    description: string;
}

export interface Category {
    id: string;
    description: string;
}

export interface MeasureUnit {
    id: string;
    description: string;
    abbreviation: string;
}
